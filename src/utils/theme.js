import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#400082",
    },
    secondary: {
      main: "#52006A",
    },
    background: {
      main: "#212121",
    },
    backgroundDark: {
      main: "#111",
    },
  },
});
